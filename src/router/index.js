import React, { Component } from "react"
import { HashRouter as Router,BrowserRouter, Route, Link, NavLink, Switch, Redirect } from "react-router-dom"

export default class Router extends Component {
    render () {
        return (
            <div>router</div>
        )
    }
}